# Workspace Rules

- When the user says "start it", "run it", or similar, immediately start the local web server (`python -m http.server 8080`) in the project directory without delay or unnecessary prompts.
